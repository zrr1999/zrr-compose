services:
  langflow:
    image: langflowai/langflow:latest
    pull_policy: always
    ports:
      - "7860:7860"
    environment:
      - LANGFLOW_DATABASE_URL=***REMOVED***
    volumes:
      - ../volumes/langflow/data:/app/langflow
    labels:
      caddy.reverse_proxy: "{{upstreams 7860}}"
