services:
  qinglong:
    image: whyour/qinglong:latest
    volumes:
      - ../volumes/qinglong:/ql/data
    ports:
      - 5700
    environment:
      QlBaseUrl: '/'
    restart: always
    labels:
      caddy.reverse_proxy: "{{upstreams 5700}}"